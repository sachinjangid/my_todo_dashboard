{
  "name": "my_todo_dashboard",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "prettier": "^1.11.1",
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-scripts": "1.1.1"
  },
  "scripts": {
    "format": "prettier --no-semi --single-quote --trailing-comma all --write 'src/**/*.js'  'public/**/*.js'",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
